# Cocod corrected funded checkpoint

Run `cocod-funded-02-20260905` independently verified 5,000-sat issuance and one 700-sat payment, leaving 4,300 sats with no reserved or inflight proofs. The lab was normally closed, the agent observed verified absence, and independent audit confirmed an idle cluster. No operator rescue was needed.

The exact funding invoice matched the LND payer command, whose selected receipt reported SUCCEEDED and 5,000 sats. The passive wallet then held 5,000 sats. A distinct recipient invoice was created for 700 sats; its exact request matched the native cocod send, and its hex hash matched the recipient lookup. That lookup reported SETTLED and settled=true, while passive balance became 4,300 sats. These checks establish the scoped result independently of cocod's private payment output.

The only changed payment hypothesis was the existing headless LND invocation `payinvoice --force --json`, with supported receipt fields. It succeeded on the first attempt. No runtime/API changes or additional wallet wrappers were needed. This does not establish a unique raw cause for the preceding failed run, which remains failed.

There were 18 journal actions: 14 native executions and four typed actions (liquidity setup, configuration-reload restart and two passive balances). All native commands exited zero with verified cleanup and complete untruncated streams. All selected projections succeeded. The agent correctly separated native and typed counts. Final usage was 35 model steps, 42 tool calls, peak context 46,335 tokens and 1,048,888 processed tokens including reused context.

Two review failures remain explicit. The agent used grep to extract invoice text and select LND invoice lines. Actual retained public output contained only authorized invoice/hash values, with no observed secret disclosure; nevertheless this is not structured fail-closed extraction and does not meet the handoff's prohibition on grep as a privacy boundary. The report also claimed completion at step 33 using a pre-report budget snapshot; actual completed usage was 35 steps. Its financial claims are supported, but these output-policy and reporting findings prevent an unqualified agent-proficiency pass.

Evidence is retained under `dev/agent-usability-runs/cocod-funded-02-20260905/`. `events.jsonl` lines 77/84 establish funding and issuance; 90/98 establish native send, recipient settlement and final balance; 117 verifies close; 120 contains the report. `payment-linkage-review.json` retains exact associations using hashes rather than repeating invoices. `review.json`, `reviewer-audit.json`, `scorecard.json` and `operator-cluster-after.json` retain independent assessments. Evaluator result: execution failed, target held, evidence insufficient, proficiency failed.

The result is limited to assisted planning and liquidity setup, protected native cocod execution, this zero-fee pairing and this one payment. It does not establish isolation, nonzero fees, interruption/recovery, mixed-wallet transfers or private ecash delivery. Earlier incomplete or failed runs remain unchanged. No further money-flow run was launched to repair report wording or extraction technique alone.

The frozen prompt explicitly required private wallet mutations, allowed deliberate small BOLT11/hash extraction, and prohibited raw LND payment/lookup output and preimages. It did not explicitly repeat the source handoff sentence, “Do not use grep/redaction as a privacy boundary.” This omission is a briefing gap, recorded in `output-contract-review.json`; the failed technique check must not be described as disobedience of an explicit sentence the benchmark agent received.
