# Private ecash Kimi 2.5 checkpoint

Run `private-ecash-agent-03-20260906` completed real native 70-sat cocod→CDK and 30-sat CDK→cocod transfers, but **the full usability checkpoint failed review**. Its independent audit verified an idle cluster after agent-observed teardown. No operator rescue or ambiguous mutation replay occurred.

The existing OpenRouter configuration advertised `openrouter/moonshotai/kimi-k2.5`, which successfully ran the session. No provider or credentials were installed or changed. Caps remained 600 seconds, 50 steps, two equivalent attempts and the existing token ceilings. Operator-assisted protected setup and real 5,000-sat prefunding were disclosed and independently verified before the model timer.

## Verified results

- Repaired preparation succeeded immediately. All six custody actions had matching restricted metadata across the actual OpenCode before-hook, proxy receipt/forward and durable MCP request. These snapshots do not reconstruct raw provider arguments before client parsing.
- Native outward transfer produced independently observed balances 4,930/70. The native return produced observed spendable balances 4,960/40.
- All four private capture/consume receipts had empty ordinary streams, verified cleanup and retired private files. Both custody references were released.
- Thirteen native receipts, including setup, retained exit zero and complete/untruncated streams with verified cleanup.
- The agent released the lease, closed the experiment, exported the complete journal, observed verified lab absence and returned a report within the cap. The operator independently confirmed idleness.

## Why the score remains failed

The final balance jobs were submitted before the corrected CDK `check-pending` command. The retained CDK snapshot has 40 spendable sats and **30 pending-spent sats**. Reconciliation subsequently succeeded, but no later passive observation established the required zero pending-spent state. This is an evidence-ordering gap, not an established wallet recovery defect.

The agent also used **public** output for `check-pending`, contrary to the private-only instruction. Restricted review matched its exact 112-byte output to recovery counts, the known lab mint URL and “No orphaned pending proofs found.” No token, credential, seed or preimage material was observed there. The output-policy violation remains; absence of a detected leak does not excuse it.

The final report presented the checkpoint as complete despite the missing state criterion, used an earlier 36-step snapshot rather than the final 38 steps, and gave an unqualified seven-native count that excludes six setup receipts. The reviewed score is **execution failed / target inconclusive / evidence insufficient / proficiency failed**. Successful transfer/custody observations remain valid subsets of that failed checkpoint.

## Exact accounting and cost

There are 29 journal actions: setup has six live executions, one restart, one liquidity bootstrap and two balances; the model has seven live executions, six custody actions and six balances. Three additional input-validation refusals occurred before journal/action creation and were corrected with changed arguments. Native receipts total 13: six setup and seven model.

The session used **347 seconds, 38 steps, 1,125,172 processed tokens and 46,525 peak context tokens**. OpenCode reported cost **0.15395656**; billing was not independently verified. The encoded-token scan found no matches in its documented public artifact scope, but is not exhaustive for every secret representation.

Evidence is under `dev/agent-usability-runs/private-ecash-agent-03-20260906/`: reviewed scorecard, journal/argument audits, restricted public-output review, privacy/cost assessments, setup receipts, transcript/final report, harness digests and independent idle audit. Controller/runner remained at their deterministic checkpoint pins; release MCP was `32375df6ea1dc3b7dd0f57857aa966554c03a748a7930d262f57b90c8b691624`.

The coordinator authorized one separate run04 with disclosed harness changes: enforce public output only for exact safe help/version argv, require successful private reconciliation before later final balance calls, and delegate final totals to the harness. That authorization does not change run03's failed grade. No further session is authorized automatically after run04.
